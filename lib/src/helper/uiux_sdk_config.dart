enum CardValidationStep { CARDFRONT, CARDBACK }

enum CardValidationType { CCCD, PASSPORT }

enum CardValidationStatus { DEFAULT, INVALID, VALID }

enum CameraStatus {
  NULL,
  INIT,
  DISPOSE,
  RESUME_PREVIEW,
  PAUSE_PREVIEW,
}
